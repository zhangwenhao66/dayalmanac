# DayAlmanac "真假判断题" —— 实施方案

**要解决的问题**：跟 WarCrumbs/MythCairn/Hollowvane/UmberLore 同一目的——把"某观察日的日期/官方状态/是否等同于另一个观察日"这类单句就能被AI答完的事实，改造成需要用户先猜再看解释的互动格式。DayAlmanac 是 GSC 实测里矩阵中曝光量最高但CTR最低、内容类型最"纯事实查询"的站，目前零互动元素，是矩阵里对这个风险最脆弱的两个站之一（另一个是LingoGrove）。

## 数据来源与选题

不新写内容，不做正则抽取。逐篇通读全部15篇文章（Observances 9篇/Birthstones 4篇/Zodiac Dates 2篇）的 `faq` 数组，只挑选**FAQ答案本身是干净是/否判断**的条目——答案开头就是明确的"Yes"/"No"/"Not officially"/"Not closely"这类词，不摘那些FAQ本身就是"取决于/有争议/两种说法都对"式的混合答案（比如 National Boss's Day 的周末顺延规则、National Dog Day 的商标状态、garnet是不是摩羯座石这几条FAQ因为答案本身就是"不确定/取决于来源"被排除，不能强行掰成二元判断）。claim直接用FAQ的question，explanation直接用FAQ的answer原文，不改写。

26条，覆盖全部15篇文章（大部分每篇2条，个别信息量较薄的1条）。

## 数据结构 + 组件

跟WarCrumbs `MythQuiz` 完全同构：

- `src/data/dayQuiz.ts`：`DayQuizItem[]`，字段 `{claim, verdict, explanation, sourceSlug, sourceTitle}`
- `src/lib/dayQuiz.ts`：`pickQuizSet()`（Fisher-Yates洗牌，非`sort(()=>Math.random()-0.5)`）+ `renderInlineMarkdown()`（**bold**/[text](url)转HTML，DayAlmanac的FAQ答案里同样有markdown链接）+ 单元测试
- `src/components/DayQuiz.astro`：vanilla `<script>` island（不写`type="module"`，Astro自己处理打包），视觉复用DayAlmanac既有的 `.panel`/`.leaf`/`.rubric` 样式类 + `--flag`/`--flag-dark`/`--ink`/`--paper`/`--rule` 变量，不是照搬WarCrumbs的金色调
- 新建 `/true-or-false/` 页面

## 验收标准

- 每条claim/explanation能在对应文章FAQ数组里逐字核对到
- `npm test` 全绿（含新增的 `src/lib/dayQuiz.test.ts`，package.json的test脚本需要扩到同时跑 `tools/**/*.test.mjs` 和 `src/lib/*.test.ts`）
- `npm run build` 无错误
- 手动/自动验证完整一轮游戏流程
- 完成后 commit + push + 确认线上生效 + IndexNow提交 + 内容发布日志追加记录
