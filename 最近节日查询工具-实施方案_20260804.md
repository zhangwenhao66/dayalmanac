# DayAlmanac 最近节日查询工具 —— 实施方案（延后执行，待词库达标自动触发）

**背景**：同其余三份文档，2026-08-04 调研发现单一事实查询页对 AI Overview 最脆弱。DayAlmanac 的日期页(尤其 W 层纯日期页)是这种格式的典型。这个工具让用户输入任意日期，动态查距离最近的观察日/节日，是需要输入才能出结果的交互，比静态单页更抗打。

**为什么延后**：截至 2026-08-04，`src/data/guides.ts` 里只有 9 条记录（8 个 Observances + 1 个 Birthstone，Zodiac Dates 分类尚未开始建）。全年365天只有9个锚点，用户查"最近的节日"大概率要等好几周才能等到一个，工具价值太薄。**触发条件：Observances + Public Holidays 两个分类合计条目数达到 50 个（含）以上时再做**。`dayalmanac-content-publishing` 任务每次运行会自动检查这个数字，达标后按本文档自动实施，不需要人再回来手动触发。

## 功能描述

用户输入任意一个日期（默认今天），工具用已有的 `dateRule`/`occurrences` 数据（已经是生成器算好的、覆盖未来至少5年的日期表，不需要重新计算日期逻辑），找出距离该日期最近的几个观察日/节日（前后各找1-3个），链接到对应的站内文章页。

**跨站边界（硬性，不能违反）**：这个工具**不做"还有几天"倒计时算术**——`独立站/dayalmanac/内容发布日志.md` 里已经记过的纪律四写得很清楚，"days until X"是 CalcBadger 的地盘。这个工具只做"最近的节日有哪些、分别是哪天"，不展示倒计时天数。

## 数据与实施

数据源：`src/data/guides.ts` 里已有的 `dateRule.occurrences`（已经算好的未来日期列表，来自 `tools/generate-dates.mjs`，是权威数据不需要重新验证）。

1. `src/lib/nearestObservances.ts` —— 纯函数，输入一个用户日期 + 全部 guide 的 occurrences 列表，输出按日期距离排序的最近几条记录。不涉及日期计算逻辑本身（那部分已经在 `generate-dates.mjs` 里验证过），只是排序/筛选，逻辑简单但仍需要单元测试覆盖跨年边界（比如12月底查询要能找到明年1月初的节日）。
2. `src/components/NearestObservanceTool.astro` —— vanilla `<script type="module">` island（不新增框架依赖），日期输入框+结果列表。
3. 接入位置：站点首页或一个独立的 `/find-nearest-observance` 页面。

## 验收标准

同其余三份文档：`npm test`全绿 + `npm run build`无错误 + 手动验证跨年边界case + commit/push + 日志记录。

## 触发与执行说明（写给 dayalmanac-content-publishing 任务）

见 `~/.claude/scheduled-tasks/dayalmanac-content-publishing/SKILL.md` 里的对应检查步骤——每次任务运行时顺手检查 Observances+Public Holidays 条目数，达到 50 个后按本文档实施一次，实施完成后从 SKILL.md 里删除那条检查指令。
